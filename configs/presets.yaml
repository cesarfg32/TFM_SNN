fast:
  epochs: 2
  batch_size: 64      # ← sube de 32 a 64 (T=10 permite más batch)
  T: 10
  gain: 0.5
  encoder: rate
  lr: 1.0e-3
  amp: true

std:
  epochs: 8
  batch_size: 56      # ← sube de 48 a 56 (si cabe, luego prueba 64)
  T: 16
  gain: 0.5
  encoder: rate
  lr: 5.0e-4         # ↓ antes 7.5e-4
  amp: true
  es_patience: 3      # ← opcional
  es_min_delta: 1e-4  # ← opcional

accurate:
  epochs: 20
  batch_size: 16
  T: 30
  gain: 0.5
  encoder: rate
  lr: 7.5e-4
  amp: true
